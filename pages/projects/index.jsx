import React from 'react';
import ProjectsPage from '../../components/widgets/projects';

function index(props) {
    return (
        <div>
            <ProjectsPage/>
        </div>
    );
}

export default index;